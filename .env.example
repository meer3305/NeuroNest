# =============================================================================
# Routine Bright Buddy – Environment variables
# =============================================================================
# Copy this file to .env and fill in your values. Never commit .env to git.
# For Vercel: set these in Project → Settings → Environment Variables.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase (required for auth, database, storage)
# Get these from: Supabase Dashboard → Project Settings → API
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key

# Backend/Supabase (for API server or Vercel serverless – uploads, service role)
# Get service role from: Supabase Dashboard → Project Settings → API → service_role
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# Optional: Supabase Storage bucket name for video uploads (default: videos)
SUPABASE_BUCKET=videos

# Optional: Enable uploading generated videos to Supabase Storage (default: false)
ENABLE_SUPABASE_UPLOAD=false

# -----------------------------------------------------------------------------
# Frontend (Vite) – only VITE_* are exposed to the browser
# -----------------------------------------------------------------------------
# Set to "true" to run without Supabase (demo mode, local data only)
VITE_DEMO_MODE=false

# Optional: Backend API base URL. Leave empty to use same-origin /api (Vercel).
# Set to http://localhost:8000 when running the Python backend locally.
VITE_BACKEND_URL=

# -----------------------------------------------------------------------------
# Backend / API (Python – server/ or api/ for Vercel serverless)
# -----------------------------------------------------------------------------
# Optional: CORS origin for API (default: *). Set to your frontend URL in production.
BACKEND_CORS_ORIGIN=*

# Optional: Override writable directory for generated videos (default: auto)
# VIDEOS_DIR=/tmp/videos

# -----------------------------------------------------------------------------
# Hugging Face / Video generation (optional – for AI-generated step videos)
# -----------------------------------------------------------------------------
# HF_TOKEN=your-huggingface-token
# HF_PROVIDER=replicate
# HF_MODEL=Wan-AI/Wan2.2-TI2V-5B

# Demo mode for video: use built-in animated generator instead of HF (default: true)
# HF_DEMO_MODE=true

# Optional: Hugging Face Space for text-to-video (alternative to HF inference)
# HF_SPACE_ID=username/space-name
# HF_SPACE_URL=https://username-space-name.hf.space

# -----------------------------------------------------------------------------
# Supabase Edge Function: generate-flashcards (AI Magic)
# Set in Supabase Dashboard → Edge Functions → generate-flashcards → Secrets
# -----------------------------------------------------------------------------
# LOVABLE_API_KEY=your-lovable-api-key
